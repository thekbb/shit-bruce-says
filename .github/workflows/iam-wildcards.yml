name: Expand IAM Wildcards

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  expand-wildcards:
    runs-on: ubuntu-latest
    steps:
      - name: Expand IAM Wildcards
        uses: thekbb/expand-iam-wildcards@5c532fe1c93e7f81e1c311441a17db36d018c128
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
