name: Expand IAM Wildcards

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  expand-wildcards:
    runs-on: ubuntu-latest
    steps:
      - name: Expand IAM Wildcards
        uses: thekbb/expand-iam-wildcards@8e65fcec867dca7d4d7cc124f387bba438b0aa18
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
