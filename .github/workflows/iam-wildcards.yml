name: Expand IAM Wildcards

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read
  pull-requests: write

jobs:
  expand-wildcards:
    runs-on: ubuntu-latest
    steps:
      - name: Expand IAM Wildcards
        uses: thekbb/expand-iam-wildcards@a328eb86c5d294a3bc93ea3c334b9f2ef669efbf
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
